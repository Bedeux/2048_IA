Dire dans les commmentaires ou dans les noms de champs que c'est bien une grid log2
S'assurer que la liste en entrée de chaque fonction soit en np array uint8 (fonction préalable de cast?)
Dans le add random tile, on fait une copie qui a l'air inutile mais Chat GPT me dit que ca me servira pour NEAT, verifier si ce sera le cas
Typer les paramètres des fonctions (exemple :  grid : ndarray)
Documenter les fonctions move pour expliquer les lookup
Créer le requirement.txt

Faire monte carlo avec bcp de simulations par coup (genre 500)
Sur Board mettre le type de la sortie attendu dans chaque fonction( -> bool  )
Faire une interface graphique ? avec animations et exemple 